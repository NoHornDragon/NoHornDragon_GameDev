using UnityEngine;

namespace NHD.Entity.NPC.Common
{
    public class NPCText : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}